import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

const originTrackerSlice = createSlice({
  name: "OriginTracker",
  initialState,
  reducers: {},
  extraReducers: {},
});

export const {} = originTrackerSlice.actions;

export default originTrackerSlice.reducer;
